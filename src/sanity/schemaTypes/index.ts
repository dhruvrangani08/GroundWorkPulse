// Basic schema types for GroundWorkPulse
import { header, footer, hero, credibilityLine, howItWorks, product, conditions, brief, vsTable, pricing, finalCTA } from './components';
import { page } from './documents/page';
import { metaSection } from './sections';

export const schemaTypes = [header, footer, hero, credibilityLine, howItWorks, product, conditions, brief, vsTable, pricing, finalCTA, page, metaSection];
