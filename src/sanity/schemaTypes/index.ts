// Basic schema types for GroundWorkPulse
import { header, footer, hero, credibilityLine, howItWorks, product, conditions, brief } from './components';
import { page } from './documents/page';
import { metaSection } from './sections';

export const schemaTypes = [header, footer, hero, credibilityLine, howItWorks, product, conditions, brief, page, metaSection];
